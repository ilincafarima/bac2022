#include <iostream>
#include <cmath>
using namespace std;

float f(float x){
    return sin(1.3*x-2)+4;
}

int n=20,a,b=3;
float s,h,x;
int main(){
    h=abs(b-a)/n;
    for(int i=0;i<n-1;i++){
        x=a+i*h;
        s=s+h*f(x);
    }
    cout<<"volumul bazinului: "<<s*4.5;
    return 0;
}
